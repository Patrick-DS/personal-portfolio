// Third-party imports

// Global imports

// Local imports

////////////////////////////////////////////////////////////////////////////////

interface RouterPaths {
    [key: string]: string
}

const routerPaths: RouterPaths = {
    home: "/",
    cv: "/cv",
    projects: "/projects",
    contact: "/contact"
}

export default routerPaths
